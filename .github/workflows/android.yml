name: NEXA Music Android Build

on:
  push:
    branches: [ main, nexa-v1 ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle
      - name: Install Gradle
        run: |
          curl -fsSL https://services.gradle.org/distributions/gradle-8.10.2-bin.zip -o /tmp/gradle.zip
          unzip -q /tmp/gradle.zip -d /opt
          echo "/opt/gradle-8.10.2/bin" >> "$GITHUB_PATH"
      - name: Verify AndroidX configuration
        run: |
          test -f gradle.properties
          grep -q '^android.useAndroidX=true$' gradle.properties
          echo "AndroidX enabled"
      - name: Build debug APK
        run: gradle --no-daemon -Pandroid.useAndroidX=true -Pandroid.enableJetifier=true assembleDebug
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: NEXA-Music-debug-apk
          path: app/build/outputs/apk/debug/*.apk
